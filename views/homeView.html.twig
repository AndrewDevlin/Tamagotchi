<!DOCTYPE html>
<html>
<head>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link href="https://maxcdn.bootstrapcdn.com/bootswatch/3.3.7/cyborg/bootstrap.min.css" rel="stylesheet" integrity="sha384-D9XILkoivXN+bcvB2kSOowkIvIcBbNdoDQvfBNsxYAIieZbx8/SI4NeUvrRGCpDi" crossorigin="anonymous">
    <link rel="stylesheet" href="styles.css">
    <title>Tamagotchi</title>
</head>
<body>
    <div class="panel panel-info">

      <h1>Your Tamagotchi</h1>
      {%if tama is empty%}
        <form action="/new" method="post">
          <label for="name">Enter Your tamagotchi's Name:</label>
          <input type="text" name="name">
          <button type="submit" name="button">Make a New Tama</button>
        </form>
      {% elseif tama is not empty%}
        <ul>
          {% for item in tama%}
          <li>Name: {{item.getName}}</li>
          <li>Food Level:
            <div class="progress progress-striped active" style="width: 30%">
              <div class="progress-bar" style="width:{{(item.getFood/30)*100}}%"></div>
              <div class="progress-bar progress-bar-danger" style="width:{{100-(item.getFood/30)*100}}%"></div>
            </div>
          </li>
          <li>Sleep Level:
            <div class="progress progress-striped active" style="width: 30%">
              <div class="progress-bar  progress-bar-warning" style="width:{{(item.getSleep/30)*100}}%"></div>
              <div class="progress-bar progress-bar-danger" style="width:{{100-(item.getSleep/30)*100}}%"></div>
            </div>
          </li>
          <li>Play Level:
            <div class="progress progress-striped active" style="width: 30%">
              <div class="progress-bar progress-bar-success" style="width:{{(item.getPlay/30)*100}}%"></div>
              <div class="progress-bar progress-bar-danger" style="width:{{100-(item.getPlay/30)*100}}%"></div>
            </div>
          </li>

      {% endfor %}
        </ul>
        <form class="interaction" action="/feed" method="post">
          <button type="submit" name="button">Feed</button>
        </form>
        <form class="interaction" action="/sleep" method="post">
          <button type="submit" name="button">Sleep</button>
        </form>
        <form class="interaction" action="/play" method="post">
          <button type="submit" name="button">Play</button>
        </form>
      {% endif %}
<br><br>
      <form action="/delete" method="post">
        <button type="submit" name="button">Delete</button>
      </form>
    </div>
</body>
</html>
